{
  "name": "whalefin-frontend",
  "private": true,
  "scripts": {
    "serve": "NODE_OPTIONS=--openssl-legacy-provider bun x webpack-dev-server",
    "build": "NODE_OPTIONS=--openssl-legacy-provider bun x webpack --mode production"
  },
  "dependencies": {
    "core-js": "^3.6.4",
    "regenerator-runtime": "^0.13.3",
    "@wailsio/runtime": "^3.0.0-alpha.62"
  },
  "devDependencies": {
    "copy-webpack-plugin": "^12.0.2",
    "webpack": "^5.99.0",
    "webpack-cli": "^7.0.2"
  }
}
